spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://smooth-database.c92iu2og4xc3.ap-northeast-2.rds.amazonaws.com:3306/pothole_analysis?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul}
    username: smooth_admin
    password: smooth_admin
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  cloud:
    aws:
      credentials:
        access-key: ${AWS_ACCESS_KEY}
        secret-key: ${AWS_SECRET_KEY}
      region:
        static: ap-northeast-2
      stack:
        auto: false